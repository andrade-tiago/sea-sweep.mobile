import { ActivityIndicator } from "react-native";

export default function Loading() {
  return (
    <ActivityIndicator className="flex-1 bg-dark_blue items-center justify-center text-gray-100" />
  )
}
